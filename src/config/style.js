export const stylePrefix = 'talqs';

export const label = `${stylePrefix}_label`;

export const panel = `${stylePrefix}_panel`;

export const tree = `${stylePrefix}_tree`;

export const clear = 'clearfix';